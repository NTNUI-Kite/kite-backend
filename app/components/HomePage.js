var React = require('react');

class HomePage extends React.Component {
  render(){
      return (
        <div>
          <h1>Home Page</h1>
        </div>
      );
  }
}

module.exports = HomePage;
